Implemented a webserver using node.js, ajax, javascript and jquery to get information about a user's
favorite places and dynamically appending them to a web page.

to run use node index.js 
Go to a browser and type http://localhost:9007
